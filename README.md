# Lightweave has moved

This project now lives at **[moda-labs/lightweave](https://github.com/moda-labs/lightweave)**.

This repository is an empty placeholder. It exists only to reserve the old name
so the path cannot be claimed by someone else and used to impersonate the
project in old links, clone URLs, or release asset URLs.

Releases from `v0.7.1` onward are published at the new location. Anything that
still points here — a checkout, a bookmark, a pinned release URL — should be
repointed at `moda-labs/lightweave`.
